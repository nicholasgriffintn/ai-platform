---
"@ngriffin_uk/polychat-library-tool-runtime": minor
---

Let a caller widen a turn's approval requirements beyond its mode's. Project task flow stages use this to make `requiresApprovalFor` real: a stage can demand a person's say-so for permissions the mode would have allowed unattended, and can never lower the mode's own bar.
