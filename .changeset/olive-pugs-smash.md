---
"@ngriffin_uk/polychat-library-client": minor
---

Add `withRetry`, `parseRetryAfterHeaderMs`, and `parseRetryAfterBodyMs` to the retry subpath. Callers supply their own retryable-status policy, so existing retry behaviour is unchanged.
