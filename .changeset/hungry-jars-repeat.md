---
"@ngriffin_uk/polychat-library-registry": minor
---

Add the shared categorised registry. `CategoryRegistry` owns registration, alias resolution, and singleton or transient lifecycle caching, and reports failures as `RegistryError` so hosts map their own error wording.
