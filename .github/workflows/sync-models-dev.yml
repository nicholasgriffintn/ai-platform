name: Sync models.dev

on:
  schedule:
    - cron: "0 5 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-models-dev:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [24.x]
    steps:
      - uses: actions/checkout@v7
      - name: Install pnpm
        uses: pnpm/action-setup@v6
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: "pnpm"
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Run models.dev sync
        env:
          POLYCHAT_API_KEY: ${{ secrets.POLYCHAT_MODEL_ANALYSIS_TASK_API_KEY }}
          POLYCHAT_API_BASE_URL: https://api.polychat.app
        run: pnpm --filter @assistant/api models:sync --write --verbose --trigger-analysis-task
      - name: Commit model updates
        id: sync-commit
        run: |
          if git diff --quiet -- apps/api/src/data-model/models; then
            echo "No model updates to commit."
            echo "committed=false" >> "${GITHUB_OUTPUT}"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/api/src/data-model/models
          git commit -m "chore(models): sync models.dev"
          commit_sha="$(git rev-parse HEAD)"
          echo "committed=true" >> "${GITHUB_OUTPUT}"
          echo "commit_sha=${commit_sha}" >> "${GITHUB_OUTPUT}"
          git push
