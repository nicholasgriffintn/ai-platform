# @ngriffin_uk/polychat-library-agent-core

Use the reusable decision loop and action handler contracts to run Polychat agents in a host
runtime. Hosts provide model decisions, action handlers, approvals, cancellation, and event
reporting through the public interface.
