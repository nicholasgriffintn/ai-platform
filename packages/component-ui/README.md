# `@ngriffin_uk/polychat-component-ui`

Accessible React DOM controls, forms, status views, and layout primitives for Polychat surfaces.

The package owns presentation only. It does not fetch data, navigate, read application stores, or
access browser globals. React and React DOM are peer dependencies.
