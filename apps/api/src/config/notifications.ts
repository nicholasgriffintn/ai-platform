/** Notification provider configuration shared by the delivery modules. */

/** How long a minted APNs provider token is reused before it is regenerated. */
export const APNS_TOKEN_LIFETIME_MS = 45 * 60 * 1000;
