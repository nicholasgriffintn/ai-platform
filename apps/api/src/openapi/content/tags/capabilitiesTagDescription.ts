import { md } from "@ngriffin_uk/polychat-utility-server/markdown";

export const capabilitiesTagDescription = md`
# Capabilities

Rich experiences and model tools a project or a person can enable.

Experiences are the curated catalogue in \`services/experiences/config\`. Function tools are not experiences: they are published, and run, by \`/tools\`.
`;
