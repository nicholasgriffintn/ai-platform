import { md } from "@ngriffin_uk/polychat-utility-server/markdown";

export const audioTagDescription = md`
# Audio

Speech-to-text and text-to-speech utilities with provider abstraction.

Every endpoint requires authentication, logs usage, and returns structured payloads so downstream chat, apps, or automation flows can chain the audio artifacts safely.
`;
