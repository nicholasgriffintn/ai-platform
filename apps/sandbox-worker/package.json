{
  "name": "@assistant/sandbox-worker",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": {
    "build:deps": "pnpm --filter \"@assistant/sandbox-worker^...\" --if-present build",
    "cf-typegen": "wrangler types",
    "ci:test": "pnpm run coverage",
    "coverage": "vitest run --coverage",
    "deploy": "pnpm run build:deps && pnpm typecheck && wrangler deploy",
    "dev": "WRANGLER_BUILD_CONDITIONS=polychat-source,workerd,worker,browser wrangler dev src/index.ts",
    "start": "pnpm --filter @ngriffin_uk/polychat-schemas build && wrangler dev src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/sandbox": "0.12.6",
    "@ngriffin_uk/polychat-library-agent-core": "workspace:^",
    "@ngriffin_uk/polychat-library-client": "workspace:^",
    "@ngriffin_uk/polychat-library-tool-runtime": "workspace:^",
    "@ngriffin_uk/polychat-schemas": "workspace:^",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "wrangler": "catalog:"
  }
}
