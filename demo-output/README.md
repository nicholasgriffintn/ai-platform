# Polychat product demo

Recorded from the full macOS display with the built Tauri app on the left and the web app on the right.

- Show live shared-project chat updates in both directions without refreshing.
- Update the project brief on the web and observe it on desktop.
- Show the sandbox workbench and its actual run outcome.

The existing local development database contains Alex Morgan (`demo@polychat.invalid`), Northstar Studio, Autumn launch and Customer portal. The desktop uses the existing account as a member of the demo workspace.

The sandbox cloned the authorised demo repository and collected its files, then failed when its API client received an empty completion. Do not present this recording as a successful completed sandbox review.

Raw full-screen footage is retained alongside the edited video. The edited segments trim pauses and add labels; project editing is accelerated to 1.5×.
