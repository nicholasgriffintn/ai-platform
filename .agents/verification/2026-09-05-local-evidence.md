# Verify local main with automated journeys

Run the pending queue against isolated local data. Checked boxes record automated evidence authorised by the user; they do not certify a deployment. Keep items pending until their remaining outcomes are confirmed.

## Results

- **Current release:** `pnpm test:e2e:release` passes all 176 browser journeys, including real sandbox execution, service controls, approval expiry, responsive Workbench behaviour, preview hardening and member authority. The visual report contains 161 matched and 116 changed captures; changed captures still require review.
- **Extended sandbox:** approval expiry passes through escalation, timeout, rejected late resolution, failed Proof and no command execution. Service restart/stop/start passes with dependency order, preview revocation and idempotency. The responsive Workbench journey passes desktop keyboard resizing, pane and collapsed-state persistence, mobile full-height presentation and focus return. Replace the SDK output callback with bounded process-log polling after the real runtime exposed an unclosed RPC stream during teardown; sandbox tests, typecheck, fixture build and the focused real-container journey pass without the callback-stream error.
- **Workbench shell:** isolated browser journeys now cover the complete queued, preparing, running, paused, waiting-for-approval, waiting-for-input, review, completed, failed and cancelled presentation matrix through real Activity, task, coordinator and sandbox boundaries. They restore active and terminal runs after reload, retain Activity and Proof after coding configuration is removed, and verify keyboard focus, live-region wording, accessible tab panels, page-background status presentation, and long-detail truncation with titled icon controls at desktop, tablet and mobile widths. Fix the live announcement separator, explicit names for collapsed run controls, and narrow-width status layout after these journeys exposed accessibility defects.
- **Service failure policy:** all eight `features/sandbox-services.spec.ts` real-container journeys pass together. They reject invalid manifests, fail closed when another declared dependency already owns the requested port, retain a timed-out startup event for an unhealthy endpoint and exercise `never`, `on_failure` and `always` restart policies through their saved caps. Refresh process state before applying restart policy because the SDK process object's initial exit code is not live; record port-collision failures in durable Activity.
- **Run controls:** all three real-container control journeys pass together. Record command, parallel-command and script outcomes before honouring a pause so Activity exposes a true safe boundary. Persist submitted and received instruction identity and ordering across reload, retain one result when cancellation races completion, explain disabled terminal controls and reject stale terminal control and instruction requests.
- **Workbench Activity:** the real pause/resume journey proves ordered conversation, plan, model, tool, setup, script, instruction and validation entries, then preserves the same entry titles and count across reload with output and cost details collapsed. Merge setup and script lifecycle pairs so recorded durations appear on completed work only, and suppress conversation transport projections when authoritative run events exist. Approval journeys expose requested, approved, rejected, escalated and timed-out transitions in order; cancelled and failed runs retain explicit terminal entries. Activity omits model reasoning and tool arguments, while the environment journey proves secret-shaped output is redacted from events, artefacts and the panel.
- **Workbench changes and files:** a new real-container multi-file journey produces configuration, binary, consumer and test changes. Changes orders configuration first and tests last, searches and steps between files, renders text and binary states, and collapses context from the keyboard at wide and narrow sizes. Files now opens bounded changed-file diff evidence, refuses to decode binary content, previews authorised text artefacts and reports unavailable content. A member can read the exact diff and log Output IDs before removal; both return 404 afterwards.
- **Preview feedback:** the hardened real-container journey passes at wide and mobile sizes through Fit, Mobile, Tablet and Desktop presets, loading and healthy UI states, replacement refresh access, revoked prior access and external opening. The service suite passes starting, unhealthy and stopped states; a separate 5.5-minute journey proves API expiry, embedded-frame removal and denial of an already-open external session. A hostile fixture frame cannot read its parent or cookies, invoke trusted controls, navigate the parent or submit an instruction. The runner's normalised service, route, viewport, region, element and annotation survive exactly once with the initiating user ID after reload.
- **Preview log audit:** the browser journey rejects its exact grants, preview URLs, cookie value, forwarding-token fields, credential shapes and container identifiers in captured console messages. Count-only scans across the corresponding local Wrangler API, sandbox Worker and SDK logs found no grants, preview cookies, forwarding tokens, JWTs, connector credentials, private container addresses, SDK URLs, screenshot bytes or DOM-selector material. No E2E sandbox containers remained afterwards.
- **Environment setup:** the strengthened real-container setup journeys pass. Activity records configuration, bounded command output and completed setup before planning; Proof retains the Polychat or repository blob revision, runtime, package manager, effective setup mode and duration. A queued run keeps its captured setup after the project changes, while a later run uses a new revision. Two committed repository fixtures produce different blob revisions; unavailable runtimes, malformed JSON and oversized definitions fail before planning. Cache journeys prove lightweight resume, full setup after resume removal, rebuild and delete through the current settings route. E2E-only sandbox quotas remove artificial pacing while leaving production defaults unchanged.
- **Delivery policy:** all ten real-container delivery journeys pass concurrently with local Git repositories and mocked GitHub and model-provider boundaries. They prove legacy mapping, new defaults, policy persistence and removal, exact rejection and approval, idempotent terminal queue redelivery, default and protected branch checks including late protection, installation/repository/member authority, quality-gate blocking, retained evidence after pull-request failure and custom local-only preparation. Preserve explicit empty repository allowlists so revocation fails closed. The derived E2E image ignores only Sandbox 0.12.9's missing session PID-file open/watch race and otherwise retains fatal uncaught-exception handling.
- **Preview hardening:** the real gateway strips upstream cookies and identifying headers, keeps safe loopback redirects on the opaque origin, rejects external and undeclared-port redirects, rejects forged project and run identities, transfers WebSocket data while authorised, closes it after revocation, denies later HTTP and revokes a separate session when the run is cancelled. The extended preview journey passes against the rebuilt local container; a separate 5.3-minute journey proves active access is denied after the real five-minute lifetime.
- **Member authority:** another member can read run and pending-approval evidence but cannot steer, resume, control services, submit instructions or resolve approval through either UI or API. Removing membership blocks the loaded embedded preview on its next request, revokes external access and prevents replacement access. Both member journeys pass.
- **Checkout:** started clean on local `main`; no branch, worktree, push or PR.
- **Build:** `pnpm build:e2e` passed.
- **Baseline:** `pnpm test:e2e:release` ran 139 Chromium journeys: 136 passed, three failed, none skipped.
- **API:** 124 tests passed across chat runs, project tasks, outputs, context budgeting and output provenance.
- **Native:** `pnpm test:mobile` passed: 105 Swift tests and six launch UI tests. Native UI coverage exercises launch only; it does not confirm the interaction or physical-device queue.
- **Theme:** all five `features/themes.spec.ts` journeys pass: day/night pair options, previews and captions; appearance changes; first-paint frame history; explicit-theme isolation; keyboard changes; persistence; reversed-pair rejection; sidebar focus and narrow placement; and rendered card colour direction. Add explicit Arrow Up/Down handling after the browser left focused native pair selects unchanged, and reposition the submenu after E2E exposed mobile overflow.
- **Cancellation:** the focused cancellation E2E passed after correcting the conversation detail schema and serialising the archive flag as a boolean.
- **Keyboard:** the focused conversation-menu E2E passed after updating navigation assertions for the current Pin-through-Delete menu.
- **Failure recovery:** the baseline exposed a duplicate user bubble because completion request validation stripped its supplied ID, and newly created conversations lacked a recoverable URL. Both fixes passed the complete failure/reload/follow-up E2E. Message repository and preparation checks passed (23 tests), including protection against foreign message IDs; schema checks passed (24 tests).
- **Model selection:** all three journeys pass, covering selected model and response-control persistence, deliberate Default, every rendered state while the real catalogue response is delayed, and explicit replacement after sign-out. Fix Send remaining enabled for an unavailable selection. Align the old sign-out fallback expectation with ADR 0011's retain, block and replace contract.
- **Drafts:** all three Chat/Work journeys passed. Fix the cursor before the submenu separator when `/model` precedes a draft. All 18 composer parser tests passed.
- **Catalogue:** guest counts, all five section links, representative access badges and both curation destinations passed.
- **Run protocol:** exact cancellation command replay returns the original receipt with the duplicate flag, and changed input returns 409. Ordered replay has stable identities, only later events, repeatable pages and an explicit snapshot reset for a future cursor.
- **Approvals:** the new network-failure/retry journey exposed optimistic replies marking the card resolved before acknowledgement. Defer the reply's resolution metadata until success. The fixed journey passed, including disabled controls while submitting and a retryable inline failure.
- **Provider parameters:** the full API run found one failure among 1,904 tests: a non-finite Bedrock thinking budget when the output allowance was omitted. Resolve the required allowance from the catalogue for budget thinking. The focused provider and parameter run passed all 46 tests; the full API rerun passed all 1,904 tests across 260 files.
- **Package checks:** the web suite passed 284 tests, shared chat passed 99, schemas passed 188 and content components passed 10. API, web and E2E typechecks passed before the latest restoration fix; repeat affected checks afterwards.
- **Questions:** the network-failure/retry journey passed with the written answer retained, retry available and submission acknowledged only after success.
- **Organisation:** pin/unpin, unread/read, group assignment/removal and keyboard shortcut guards passed across reloads.
- **Conversation organisation:** all three journeys pass, adding snooze removal, global-search recovery and clearing, project-member group visibility with owner-only 403 enforcement, and the local-only Rename/Delete menu. Fix cached remote conversations bypassing the server's snooze filter while retaining the active result opened from search.
- **Project tasks:** proposed pipeline stages survive later pipeline edits and reloads; cancelling, reopening and deleting an untouched task passed. A queued task continues after its initiating page closes and reopens the exact waiting run with its questions.
- **Release rerun:** 156 of 157 browser journeys passed; model restoration passed, but council selection failed intermittently. Three focused council repeats passed; retain the full-run failure for investigation.
- **Work resume:** the complete page-close, waiting-run recovery, offline answer failure, retry and review journey passed. Await the real answer acknowledgement and allow offline requests to fail explicitly. Reopen settled usage reservations with a fresh identity and fence late settlement against the previous identity.
- **Sandbox:** the real Worker and container now clone an isolated fixture, execute a repository edit, validate it, persist private artefacts and restore Proof after reload. An unrelated signed-in account receives 404 for the same diff URL. The sandbox, task, model and recovery run passed all 18 journeys. Fix caller-owned tool calls being executed by the API, an unserialisable cancellation signal crossing Sandbox RPC, the interpreter ignoring its requested working directory and missing saved Work conversation URLs.
- **Latest checks:** API, app and sandbox typechecks passed. Sandbox tests passed 76 tests across 14 files; the caller-owned tool regression passed alongside 27 existing agent-loop tests. Repeat broader checks after the remaining changes.
- **Sandbox extensions:** JavaScript, Python and intentional failed-validation journeys passed, including reload and non-member artefact denial. Use the Python-enabled production image because the base image lacks the advertised interpreter. The environment settings journey passed save/reload, setup execution and removal without disconnecting the repository. Work resume also confirms credit reservations return to zero while waiting and after review.
- **Latest full suites:** API passed 1,905 tests across 260 files; web passed 286 tests across 70 files. E2E typechecking passed using the app workspace's TypeScript executable.

- **Shell refactor sweep (7 September):** the first release run after the desktop shell and places work failed 34 of 210 journeys. Most were the harness still naming surfaces that moved: the project library link is Teammates rather than Teammates & tools, apps open from that library instead of a separate Experiences list, project outputs live under Files > Made, project configuration sits behind the gear, the add menu offers Hire a teammate and Build one from scratch, the task dialogs say teammate rather than agent, the personal library heading is Teammates, the profile Sources and Tasks tabs redirect to Files and Attention, the pet is a link rather than a button, and the not-found page returns to /chat. All are now followed. The suite passes in batches; a single 220-journey run still loses the Wrangler proxy partway, which is the known instability rather than a product failure.
- **Product fixes found by those journeys:** the catch-all route served the not-found page with HTTP 200, so no removed path actually answered 404; it now returns 404. The project Files tab registered its own page header and replaced the project name in the shell header; it no longer does when a project header is supplied. The conversation header only offered Threads on the conversation that had just been branched from, and lost it on reload, because `has_branches` was only ever set optimistically on the parent; the conversation response now reports family membership and a stored thread carries it.
- **Coverage added (7 September):** new local Chromium journeys for the places and mode routing, the project home and its gear, the public apps catalogue and its deep links, the access token staying out of `localStorage`, keeping a message for later, the models page structure and provider marks, the composer height cap, the flush task queue with its suggested pipeline, and filing a message as a project task. Each pending item carries what its journey proved and what it left open.
- **Test races removed:** the mobile sidebar journey no longer depends on the pre-hydration flash of the drawer, and its capture stays on one breakpoint; the document editor journey waits for the save response before reading the revision; the refresh-recovery journey now requires at most one concurrent replay rather than exactly one. Three journeys still fail only under four-worker contention and pass on their own: anonymous credit streaming, the muted Live session, and two sandbox service and control journeys losing the Wrangler socket.

- **Second pass (7 September):** new journeys cover keeping a message for later, the models page and provider marks, the composer height cap, the flush task queue and its suggested pipeline, filing a message as a project task, document revisions and export refusals, the pricing credit bands, hiring a teammate from a role or a description, the internal-tool starter, and the teammates route replacing agents. All pass against the rebuilt shell.
- **Findings from that pass:** the teammate model refusal and the two retired chat paths are fixed below. `/models` still carries two level-one headings inside the main region, the shell chrome title and the page heading, which is a shell-wide decision rather than a models-page one. The mouse click on the `@` menu is still open.
- **Suite shape (superseded):** these batches were needed because the app Worker died partway through larger runs. The cause was the worker count, corrected below, and the suite now runs whole.

- **Build cache (7 September):** package builds could leave `dist` behind the source. Each package ran two commands, the bundler and the stylesheet build, and both declared `dist/**` as their output, so a cache hit on one replayed its snapshot over the other's fresh output. Reproduced deterministically by editing one source file and watching the emitted module revert. Each command now declares only the files it owns. Several earlier diagnoses in this queue were made against stale bundles and were re-checked afterwards.
- **Suite stability (7 September):** the app Worker dying partway through a run was load, not code. At four Playwright workers `wrangler dev` loses its proxy connection and every later journey fails to connect; at two workers the same set passes. The default is now two, and a single full run of 235 journeys completed with one unrelated failure in 15 minutes, against 148 failures and a dead Worker at four.
- **Fixes from this pass:** a teammate with no pinned model was refused with 400 when mentioned and now falls back to the account default; `/chat/capabilities` and `/chat/experiences` answered 200 because `/chat/:completionId?` matches any segment, and each now renders the not-found page.
- **Journeys corrected:** the sandbox preview journey expected a preview-specific loading state during a paused reload; the workbench actually shows the workspace loading state until the run resolves, and the journey now pins that. The theme migration journey polls for the client-side migration rather than reading storage before the store has evaluated.
- **Still open:** selecting a teammate from the composer's `@` menu with a real mouse click does nothing, while the keyboard and a synthetic click both work. Two candidate fixes were tried and reverted. Any side-effecting statement inside the handler makes the click work, which points at the React Compiler memoising the selection callback rather than at the menu markup.

## Environment and remaining evidence

- The member review journey passes: current membership permits service review, runner-only controls and feedback stay disabled, direct control and instruction requests return 403, and removing membership revokes existing preview access and denies replacement access. Both command approval journeys pass, including rejection before execution and a conflicting second resolution. E2E typechecking passes with these additions.
- The next full release run passed 115 of 169 journeys before failures from an outdated title-provider fixture and another lost Wrangler proxy connection. Update the Workers AI and Responses title fixtures for the current model tiers; all five affected focused conversation and billing journeys now pass. Repeat the full suite with the new approval and membership journeys before claiming release completion.

- The post-tier sandbox/model run passed 13 of 14 journeys and exposed the app's CSP blocking the embedded preview. Permit only the configured preview wildcard host through `VITE_SANDBOX_PREVIEW_HOST`; 19 focused security-policy tests pass. The extended preview rerun passes external and embedded access, partitioned cookies, signature/origin/replay rejection, service-principal enforcement, revocation and persisted review feedback. The latest API and web suites pass 1,902 tests across 258 files and 290 tests across 71 files; sandbox passes 80 tests across 15 files. Start a fresh full release run after these fixes.

- Sandbox supervision and cache passed three complete journeys covering pause/resume, pause/cancel and snapshot reuse/rebuild/delete. The initial preview journey passed real gateway access, replay rejection and HTTP revocation. Sandbox tests passed 80 tests across 15 files. Rebuild all package consumers after the shared checkout's model-tier update before repeating the wider suites.

- Run the documented test runtime with access to Wrangler's local registry. The sandboxed attempt failed during startup; the same command succeeded with sandbox approval. Native tests similarly required access to CoreSimulator.
- One extended release run lost Wrangler's proxy connection after 162 of 176 journeys, causing 14 downstream connection failures. All 14 affected journeys passed together against a fresh runtime. Remove only the validation run's orphan sandbox containers; the following clean 176-journey release run remained stable. No alternate host, port or runtime workaround was used.
- Visual capture runs only at declared checkpoints. A focused recovery run produced six changed snapshots at `https://pvc.pashi.app/builds/2f9a93aa-46b4-4be7-844a-1e82be0d23fa`; these still require review. Tests without checkpoints report no snapshots. Do not treat functional passes as contrast, typography, animation or first-paint evidence.
- Physical iPhone checks require the device, accessibility tools and matching app/API releases. Simulator launch tests cannot establish APNs delivery, thermal behaviour or VoiceOver usability.
- Use the real sandbox Worker and Cloudflare Sandbox container in the local E2E runtime. The first container journey passes with a local fixture repository and mocked GitHub/model-provider boundaries. Extend this harness for service, cache, delivery, controls and preview checks; live GitHub access is not a prerequisite.
- Live notification delivery requires the configured notification gateway, VAPID/APNs material and suitable installations. Production telemetry requires the documented 28-day cohort and minimum sample counts.
- Continue adding local browser coverage for the remaining Chat, Work, model selection, catalogue and theme outcomes. Leave combined web/iPhone checks unchecked when only the web portion is established.
- Native select arrow-key changes could not be reliably driven through the macOS headless Chromium popup. Tab order, selection through the control API, radio preservation and persisted values pass; leave the complete native keyboard step open.
